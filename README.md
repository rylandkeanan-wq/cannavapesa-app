# Cannavapesa

Static, no-build-step site (plain HTML/CSS/JS). Drag-and-drop deployable to Netlify, Vercel, or any static host.

Before launch: open `script.js` and set `WHATSAPP_NUMBER` to the real order-taking number.
